import React from 'react';
import CartList from '@/components/CartList';

const cart = () => {
  return (
    <div className='container mx-auto'>
      <CartList />
    </div>
  );
};

export default cart;
